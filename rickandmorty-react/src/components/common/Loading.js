import ClipLoader from "react-spinners/ClipLoader";

const override =  {
     display: "block",
     margin: "0 auto",
     borderColor: "red",
}


 

//
const Loading =()=> {<ClipLoader color="black"cssOverride={override} size={150} />}

export default Loading;